spring:
  datasource:
    url: jdbc:mysql://localhost:3306/my-micro
    username: root
    password: root
  jpa:
    properties.hibernate.dialect: org.hibernate.dialect.MySQLDialect

    hibernate:
      ddl-auto: update
      generate-ddl: true
      show-sql: true
